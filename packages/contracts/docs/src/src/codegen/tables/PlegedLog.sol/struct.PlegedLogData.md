# PlegedLogData

```solidity
struct PlegedLogData {
    bool reinvest;
    address user;
    uint256 pay;
    uint256 height;
    uint256 time;
    uint256 closeHeight;
    uint256 closeTime;
    PlegedStatus status;
    uint256 income;
}
```

